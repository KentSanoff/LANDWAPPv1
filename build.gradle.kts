// build.gradle.kts im Projekt-Root
plugins {
    // leer lassen oder spezielle Plugins wie Versionsverwaltung etc.
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
